# Phase 0 — تثبيت المشروع وتجهيزه للتطوير المستقر

تاريخ الإنجاز: 10 أغسطس 2026

## الهدف

كان هدف Phase 0 تحويل Studia إلى مشروع يمكن تثبيته وتشغيله واختباره من نسخة نظيفة بصورة متكررة، مع قاعدة بيانات قابلة للترحيل، بيانات تجريبية، واختبارات وعمليات CI واضحة قبل البدء بمتطلبات الإطلاق اللاحقة.

## إعداد بيئة التطوير

- وثّقنا المتطلبات الأساسية: Node.js 22.13 أو أحدث، Python 3.11 أو أحدث، PostgreSQL، pgvector، وRedis 7.
- أضفنا خطوات تشغيل منفصلة للواجهة الأمامية والخادم، بالإضافة إلى تشغيل المشروع بواسطة Docker Compose.
- وفّرنا ملفات `.env.example` آمنة للواجهة والخادم بدون مفاتيح أو أسرار حقيقية.
- تحققنا من تثبيت حزم الواجهة من ملف القفل باستخدام بيئة نظيفة.
- تحققنا من تثبيت جميع حزم Python المثبتة بالإصدارات المحددة داخل بيئة افتراضية نظيفة.
- وثّقنا روابط التطبيق المحلي وواجهة OpenAPI والخطوات المطلوبة لتشغيل الخدمات.

## قاعدة البيانات والترحيلات

- اعتمدنا PostgreSQL مع pgvector بدل قواعد البيانات المؤقتة غير المتوافقة مع خصائص المشروع.
- تحققنا من قدرة Alembic على ترقية قاعدة بيانات فارغة إلى `head` عبر جميع الترحيلات.
- تحققنا من وجود Alembic head واحد وعدم وجود تفرعات غير مقصودة في سلسلة الترحيلات.
- شغّلنا `alembic check` للتحقق من عدم وجود اختلاف غير موثق بين نماذج SQLAlchemy والمخطط.
- اختبرنا downgrade ثم upgrade لآخر ترحيل للتأكد من سلامة مسار الرجوع العملي.
- استخدمنا قاعدة PostgreSQL 18 مع pgvector للتحقق المعزول من المخطط والاختبارات.

## بيانات العرض التجريبية Seed

- أضفنا ملف Seed idempotent يمكن تشغيله أكثر من مرة بدون تكرار البيانات.
- ينشئ الـ Seed مستخدم عرض، موضوع `Biology Foundations`، وملاحظة `Photosynthesis`.
- جعلنا الملف يعمل بالطريقتين:

```powershell
python -m scripts.seed_demo
python .\scripts\seed_demo.py
```

- أصلحنا مسار استيراد Python عند تشغيل الملف مباشرة حتى يتمكن من الوصول إلى حزمة `app`.
- استبدلنا البريد القديم `demo@studia.local` لأنه مرفوض بواسطة `EmailStr`.
- أضفنا ترقية تلقائية للحساب القديم بدل إنشاء حساب عرض مكرر.
- اختبرنا الـ Seed أكثر من مرة وتأكدنا من خاصية idempotency.
- بيانات الدخول الحالية:

```text
Email: demo@studia-demo.com
Password: studia-demo-2026
```

- اختبرنا تسجيل الدخول إلى API فعليًا بهذه البيانات وحصلنا على `HTTP 200`.

## استقرار الواجهة الأمامية

- جعلنا ESLint يمر بدون أخطاء أو تحذيرات.
- جعلنا production build ينجح باستخدام Vinext 0.0.50.
- استبدلنا اختبار starter template القديم باختبارات تعكس واجهة Studia الحقيقية.
- أضفنا ثلاثة اختبارات للصفحة التعريفية، نموذج المصادقة، وواجهة التطبيق بعد تسجيل الدخول.
- أصلحنا تحذير Vite Fast Refresh الناتج عن تصدير `ThemeProvider` و`useTheme` من الملف نفسه، وفصلنا سياق الثيم في ملف مستقل.
- حسّنا تحقق نموذج الدخول قبل الطلب، مع تنظيف البريد وتحويله إلى lowercase ورسائل خطأ أوضح.
- منعنا إرسال صيغ البريد غير الصالحة كطلبات تنتهي بخطأ `422`.
- أبقينا دعم الوضع الداكن ومسارات التطبيق وسلوك API كما هي.

## اختبارات المتصفح والتكامل

- أضفنا اختبارات Playwright لمسارات التسجيل وتسجيل الدخول والرحلات الأساسية داخل التطبيق.
- غطّينا إنشاء الموضوعات، رفع المستندات، محادثة AI، الاختبارات القصيرة، والبطاقات التعليمية من خلال اختبارات الواجهة وSmoke tests للخادم.
- أضفنا اختبارات mobile viewport للامتحانات، محرر Workspace، البطاقات، Mind Map، وKnowledge Graph.
- شغّلنا الاختبارات على Chromium لسطح المكتب ومحاكاة Pixel 7.
- النتيجة المسجلة: 7 اختبارات ناجحة، مع تخطي واحد مقصود على سطح المكتب لاختبار خاص بالموبايل.

## استقرار الخادم الخلفي

- ثبّتنا بنية FastAPI والوحدات المطلوبة للتسجيل، الموضوعات، الملاحظات، المستندات، AI، الاختبارات، الامتحانات، البطاقات، والتحليلات.
- تحققنا من عمل جلسات المستخدم المخزنة في PostgreSQL باستخدام cookie آمنة بدل تخزين بيانات الجلسة في المتصفح.
- شغّلنا مجموعة اختبارات الخادم كاملة؛ النتيجة المسجلة هي 172 اختبارًا ناجحًا.
- أضفنا Smoke tests لمسارات تسجيل الدخول، الموضوعات، الملاحظات، دورة فهرسة المستند، AI fallback، إنشاء الاختبارات، مراجعة البطاقات، والتصدير.
- تحققنا من عمل الاختبارات على PostgreSQL حقيقي مع pgvector، وليس SQLite.
- حسّنا التحقق من الأخطاء المتعلقة بالمصادقة وميّزنا بين `422` للتحقق من المدخلات و`401` لبيانات الدخول غير الصحيحة.

## الأداء والموثوقية أثناء التطوير

- تحققنا من production build بدل الاعتماد فقط على Hot Module Reload.
- أبقينا فهرسة المستندات كعملية خلفية حتى لا ينتظر المستخدم انتهاء استخراج النص والـ embeddings.
- احتفظنا بمسار BM25 fallback عندما لا يكون مزود embeddings متاحًا.
- راجعنا رسائل Vite وفصلنا أخطاء التطبيق عن أخطاء إضافات المتصفح؛ رسالة `message channel closed` مصدرها عادة إضافة Chrome أو Edge وليست من كود Studia.

## Continuous Integration والأمان

- أضفنا GitHub Actions لتثبيت حزم الواجهة، lint، build، وتشغيل الاختبارات.
- أضفنا خطوات CI لتثبيت حزم الخادم وتشغيل migrations والاختبارات.
- أضفنا PostgreSQL وpgvector كخدمات داخل CI.
- أضفنا التحقق من Alembic empty upgrade وsingle head وmodel drift.
- أضفنا فحص تبعيات الواجهة باستخدام npm audit.
- أضفنا فحص تبعيات Python باستخدام pip-audit.
- أضفنا Dependabot لتحديث التبعيات دوريًا.
- أضفنا Gitleaks لفحص الأسرار المسربة.
- تحققنا من صحة ملفات YAML الخاصة بالـ CI.

## نتائج التحقق النهائية المسجلة

| الجزء | النتيجة |
|---|---|
| Frontend ESLint | ناجح، بدون تحذيرات |
| Frontend unit/render tests | 3 اختبارات ناجحة |
| Frontend production build | ناجح |
| Playwright | 7 ناجحة، 1 تخطي مقصود |
| Backend tests | 172 اختبارًا ناجحًا |
| Alembic empty upgrade/check | ناجح |
| Migration downgrade/upgrade | ناجح |
| Clean dependency installation | ناجح للواجهة والخادم |
| Demo seed | ناجح وidempotent |
| Demo API login | `HTTP 200` |

## المتبقي لإغلاق Phase 0 رسميًا

التنفيذ البرمجي للمرحلة مكتمل. يتبقى إجراء إداري خارجي واحد: تشغيل GitHub CI بنجاح على المستودع، ثم تفعيل Branch Protection على فرع `main` وجعل فحوصات CI المطلوبة شرطًا للدمج. لا يمكن فرض هذا الإعداد من ملفات المشروع وحدها، لأنه يحتاج صلاحية إدارة المستودع على GitHub.

